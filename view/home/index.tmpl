{{define "title"}}Cheer{{end}}
{{define "head"}}{{end}}
{{define "content"}}
                <div class="row justify-content-center" style="margin: 5em auto 5em;">
                    <div class="col-12">
                        <form action="/up" method="post">
                            <div class="form-row justify-content-center">
                                <div class="col-5">
                                    <div class="input-group mb-2">
                                        <div class="input-group-prepend">
                                            <div class="input-group-text">I am</div>
                                        </div>
                                        <input type="text" class="form-control" name="identity" placeholder="your name / ircnick / kerberos account">
                                    </div>
                                </div>
                                <div class="col-1">
                                    <button type="submit" class="btn btn-primary mb-2">Submit</button>
                                </div>
                            </div>
                            <input type="hidden" name="_token" value="{{$.token}}">
                        </form>
                    </div>
                </div>
{{template "footer" .}}
{{- end}}
{{define "foot"}}{{end}}